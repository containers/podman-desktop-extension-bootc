// Create a struct that represents:
// image information (ImageInfo)
export interface BootcBuildOptions {
  name: string;
  tag: string;
  engineId: string;
  type: string;
  folder: string;
  arch: string;
}
